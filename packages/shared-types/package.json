{
  "name": "@wave-code-chat/shared-types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types for Wave VSCode Chat",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "node -e \"const fs=require('fs'); ['dist'].forEach(d => {try{fs.rmSync(d, {recursive:true, force:true})}catch(e){}})\"",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "wave-agent-sdk": "file:../../wave-agent-sdk-0.0.8.tgz"
  }
}